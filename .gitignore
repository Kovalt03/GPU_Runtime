# Build artifacts (FetchContent unpacks googletest here too)
build/
build-check/
compile_commands.json

# clangd index
.cache/

# Render output — only .gitkeep is tracked
output/*
!output/.gitkeep

.DS_Store
