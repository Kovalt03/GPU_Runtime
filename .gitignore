# Build artifacts (FetchContent unpacks googletest here too)
build/
compile_commands.json

# clangd index
.cache/

# Rendered images and accumulated runs — only .gitkeep is tracked
test/benchmark/output/*
!test/benchmark/output/.gitkeep

.DS_Store
